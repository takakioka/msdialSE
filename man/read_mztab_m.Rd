% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_mztab_m.R
\name{read_mztab_m}
\alias{read_mztab_m}
\title{Read an MS-DIAL mzTab-M file and split into each table (MTD/SMH/SML/SFH/SMF)}
\usage{
read_mztab_m(
  filename,
  sep = "\\t",
  na_strings = "null",
  quote = "",
  fill = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{filename}{Path to the mzTab-M file.}

\item{sep}{Field separator (default tab).}

\item{na_strings}{Value(s) treated as NA when reading the file (default \code{"null"}).}

\item{quote}{Quote argument for \code{read.table} (default \code{""}).}

\item{fill}{Logical. Passed to \code{read.table} (default TRUE).}

\item{verbose}{If TRUE, prints messages.}
}
\value{
A list with elements:
\itemize{
\item \code{raw}: full raw table as read (data.frame with V1..Vn)
\item \code{mtd}: metadata rows (data.frame) or NULL
\item \code{smh}: SMH header (named 1-row data.frame) or NULL
\item \code{sml}: SML table (data.frame) or NULL
\item \code{sfh}: SFH header (named 1-row data.frame) or NULL
\item \code{smf}: SMF table (data.frame) or NULL
\item \code{assay_map}: named character vector (names=assay id, values=display name), possibly length 0
\item \code{study_variables}: list with:
\itemize{
\item \code{groups}: character vector like "study_variable\link{1}"
\item \code{names}:  character vector of group names (V3 for each group)
\item \code{assay_refs}: named list mapping each group -> character vector of assay ids
}
\item \code{has_sml}: TRUE/FALSE
\item \code{has_smf}: TRUE/FALSE
}
}
\description{
This function reads an mzTab-M file exported by MS-DIAL and returns each
major section as a separate data.frame:
\itemize{
\item \code{mtd}: metadata lines (MTD)
\item \code{smh}/\code{sml}: small molecule table header/body (SMH/SML) if present
\item \code{sfh}/\code{smf}: small molecule feature header/body (SFH/SMF) if present
}
}
\details{
In addition, it extracts useful helper mappings from metadata:
\itemize{
\item \code{assay_map}: mapping of assay id (e.g., "assay\link{1}") to assay name/file label (V3)
\item \code{study_variables}: a list of study_variable groups, their names, and assay_refs
}
}
