% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalize.R
\name{normalize_se}
\alias{normalize_se}
\title{Normalize an SE}
\usage{
normalize_se(
  se,
  method = c("none", "log2", "sum", "median", "zscore"),
  assay = "abundance",
  control = list()
)
}
\arguments{
\item{se}{A SummarizedExperiment.}

\item{method}{One of c("none","log2","sum","median","pqn","quantile","qc_loess").}

\item{assay}{Assay name to read/write (default "abundance").}

\item{control}{A named list of method-specific options (see Details).}
}
\value{
The SE with normalized assay (overwrites the specified assay).
}
\description{
A unified interface to normalize a SummarizedExperiment assay by various strategies.
Methods implemented: "none", "log2", "sum", "median", "pqn", "quantile", "qc_loess".
}
\details{
Method-specific controls:
\itemize{
\item log2:      \code{offset} (numeric, default 1e-9)
\item sum:       \code{target} ("median" or numeric; default "median")
\item median:    \code{target} ("median" or numeric; default "median")
\item pqn:       \code{reference} ("median" or numeric vector length = nrow), \code{pre_tic} (logical, default TRUE)
\item quantile:  \code{impute} ("halfmin" or NA), requires preprocessCore
\item qc_loess:  \code{qc_label} (value in colData$class that denotes QC; default "QC"),
\code{order_col} (colData column name for injection/run order; required),
\code{span} (loess span, default 0.75), \code{log_offset} (default 1e-9)
}
}
